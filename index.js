/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Opening from './screens/opening';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Opening);
